<div align="center">
  <img width="200" height="200" alt="logo_main" src="https://github.com/user-attachments/assets/e08abb10-9e76-4316-9a72-6c53f7f8b22c" />
  <h1><b>Dockless</b><br><br><i>“Run Python scripts without Docker. Almost.”</i></h1>
</div>

> [!CAUTION]
>![Uploading logo.png…]()

> # **ПРОЕКТ ПОКА В РАБОТЕ!**

## 🚀 Для чего использовать?

**Dockless** — это лёгкий хостинг Python-скриптов для тех случаев, когда Docker — это слишком тяжело, а «просто запустить скрипт» — уже недостаточно.

Если вам нужно управлять большим количеством `.py`-скриптов, которые работают в фоне, по расписанию или как мини-сервисы — Dockless создан именно для этого.

Проект ориентирован на домашние серверы, песочницы и pet-проекты (в первую очередь под Windows). Его ключевая идея — **"недоконтейнеры"**: изолированные процессы без виртуальных машин, образов и лишнего оверхеда.

👉 Проще говоря:

* **почти Docker**
* **но без Docker**
* **и без боли**

Дополнительно Dockless умеет **скачивать проекты напрямую с GitHub**, после чего их можно сразу запускать, останавливать и хостить.

### Примеры применения:

* 🤖 **Telegram-боты**
* 🛠️ **Микро-сервисы**
* ⚡ **Фоновые и автоматизированные скрипты**

## 🛠️ Необходимые инструменты для запуска

Для работы понадобятся:

* 🐍 **Python** — основной рантайм
* 🔗 **Git** — для загрузки репозиториев

## 📦 Библиотеки Python

Убедитесь, что установлены следующие зависимости:

```bash
pip install flask gitpython psutil
```

Используемые в проекте модули:

```python
from flask import Flask, abort, redirect, url_for, jsonify, request, render_template
from git import Repo, Git
import subprocess
import platform
import psutil
import time
import sys
import os
```
