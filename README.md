## 🚀 Для чего использовать?

Если вам нужно хостить большое количество `.py` скриптов, которые работают в фоне или периодически, этот проект станет отличным решением.
Проект ориентирован на домашние Windows-серверы и песочницы. Главная “фишка” в том, что **в отличие от Docker, приложение не создаёт виртуальные машины**, что делает его лёгким и быстрым для запуска и управления скриптами. Кроме того, программа **позволяет напрямую загружать проекты с GitHub**, после чего их можно сразу запускать и хостить.

Примеры применения:

* 🤖 **Телеграм-боты**
* 🛠️ **Микро-сервисы**
* ⚡ **Кастомные скрипты**

## 🛠️ Необходимые инструменты для запуска

Для работы понадобятся:

* 🐍 **Python**
* 🔗 **Git**

## 📦 Библиотеки Python

Убедитесь, что установлены следующие зависимости. Пример установки через pip:

```bash
pip install flask gitpython psutil
```

Импортируемые модули в проекте:

```python
from flask import Flask, abort, redirect, url_for, jsonify, request
from git import Repo, Git
import subprocess
import platform
import psutil
import time
import sys
import os
```
